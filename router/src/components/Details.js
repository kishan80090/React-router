import { Link } from "react-router-dom";

function Details()  {
    return(
        <div>
            <h1>signup</h1>

            <Link to="/"></Link>
            <button><Link to="/signup">Sign in</Link></button>

        </div>
    )
};
export default Details;